window.onload = function() {
    // Attendre la fin de l'animation du logo (3 secondes)
    setTimeout(function() {
        // Rediriger vers la page d'accueil
        window.location.href = "home.html"; // Remplacez "home.html" par le nom de la page cible
    }, 3000);
};
